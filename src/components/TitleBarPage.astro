---
const { name, href, active } = Astro.props;
---

<div class="flex flex-col group cursor-pointer">
    <a class="text-xl font-bold align-middle duration-300" href={href}>{name}</a>
    <div class={`h-1 w-4 group-hover:w-full ${active ? "bg-orange-500" : "bg-green-500"} group-hover:bg-sky-400 duration-300`}></div>
</div>